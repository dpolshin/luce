package foo.bar.luce;

import foo.bar.luce.model.Token;

import java.util.LinkedList;

public class TokenFilterChain {

    private LinkedList<TokenFilter> filterChain;

    Token filter(Token token) {
        //todo;
        return token;
    }
}
